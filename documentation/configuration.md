# Configuration reference

Every environment variable the webapp reads, where it is set, and what happens when it is missing.

**This table is the authoritative list.** The code side of it is a single file —
`webapp/mc-web/src/main/kotlin/app/mcorg/config/ConfigLoader.kt` — and `readConfig()` there is the
only place in `src/main` that calls `System.getenv`. If you add a variable, add it in both places.

Written for MCO-333, which found nine declaration sites and no list: `MODRINTH_URL` declared while
the code read `MODRINTH_BASE_URL`, `TEST_HOST` and `KTOR_LOG_LEVEL` declared but never read, and
several variables read but declared nowhere.

## How failures behave

Configuration is read once at startup and validated by `AppConfig.initOrExit()`, the first
statement of both `main()` entry points (the web app and the ingestion CLI). Three outcomes:

| Severity | When | Behaviour |
| --- | --- | --- |
| **Fatal everywhere** | `ENV` is set to something that does not parse | Log and `exit 1`, in every environment including LOCAL. The environment selects the security posture, so an unparseable value has no safe default (MCO-331). |
| **Fatal outside LOCAL** | A required variable is missing | Log and `exit 1` before Netty binds. |
| **Warning in LOCAL** | A required variable is missing | Log a warning and continue, so a fresh clone runs without a full env file. |

"Required" below means required in the environments named — the app exits at startup without it.

## Variables

Legend: **S** = secret (never in a committed file; Fly secret or GitHub secret).

| Variable | S | LOCAL | TEST | PRODUCTION | Default | Set in |
| --- | --- | --- | --- | --- | --- | --- |
| `ENV` | | optional | required | required | `LOCAL` when absent | `local.env`, `test.env`, `fly.toml`, `dev.fly.toml`, `ingest-machine.sh`, `prod.yml` |
| `DB_URL` | | required | required | required | — | `local.env` (per-worktree), `test.env`, `fly.toml`, GitHub `production` var |
| `DB_USER` | | required | required | required | — | as `DB_URL` |
| `DB_PASSWORD` | S | required | required | required | — | `local.env`, `test.env`, Fly secret, GitHub secret |
| `SKIP_MICROSOFT_SIGN_IN` | | optional | optional | optional | `false` | `local.env`, `test.env`, `fly.toml`, `ingest-machine.sh` |
| `MICROSOFT_CLIENT_ID` | S | required¹ | required¹ | required¹ | — | `local.env`, Fly secret |
| `MICROSOFT_CLIENT_SECRET` | S | required¹ | required¹ | required¹ | — | `local.env`, Fly secret |
| `RSA_PRIVATE_KEY` | S | unused² | required | required | — | Fly secret |
| `RSA_PUBLIC_KEY` | S | unused² | required | required | — | Fly secret |
| `APP_HOST` | | unused³ | required | optional | `app.seam.gg` in PRODUCTION | `test.env`, `fly.toml` |
| `PREVIEW_PASSWORD` | S | unused | **required** | unused | — | `test.env`, `dev.yml` |
| `DEMO_USER` | | optional | optional | optional | none⁴ | `local.env.example`, `test.env` |
| `WEBHOOK_ADMIN_SECRET` | S | optional | optional | optional⁵ | — | Fly secret (currently unset) |
| `SEAM_DISCORD_URL` | | optional | optional | optional | — | `fly.toml` |
| `SEAM_WEBHOOK_SHARED_SECRET` | S | optional | optional | optional⁶ | — | Fly secret |
| `MICROSOFT_LOGIN_BASE_URL` | | optional | optional | optional | `https://login.microsoftonline.com` | `fly.toml`, overridden per-test by WireMock |
| `XBOX_AUTH_BASE_URL` | | optional | optional | optional | `https://user.auth.xboxlive.com` | `fly.toml` |
| `XSTS_AUTH_BASE_URL` | | optional | optional | optional | `https://xsts.auth.xboxlive.com` | `fly.toml` |
| `MINECRAFT_BASE_URL` | | optional | optional | optional | `https://api.minecraftservices.com` | `fly.toml` |
| `LAUNCHER_META_BASE_URL` | | optional | optional | optional | `https://launchermeta.mojang.com` | nowhere — default is the intended value |
| `FORCE_REINGEST` | | optional | optional | optional | — | ad hoc, per ingestion run |

¹ Not required when `SKIP_MICROSOFT_SIGN_IN=true`, which is the normal LOCAL and ingestion setting.

² LOCAL deliberately leaves the RSA pair unset: `jwt.kt` falls back to the PEM pair generated by
`mc-web/create-keys.sh` into `resources/keys`. That is the intended local path, not a
misconfiguration.

³ `getHost()` returns null in LOCAL, so the auth cookie is host-less and the OAuth redirect is
unused.

⁴ No default since MCO-333. It previously fell back to the literal `"evegul"`. With `DEMO_USER`
unset, demo sign-in fails closed and redirects to `/auth/sign-in?error=demo_sign_in_not_configured`.

⁵ Deliberately unset in production (decision recorded 2026-08-15). It is the sole gate on the
machine-facing `POST/DELETE /integrations/webhooks` routes, which are JWT-exempt — and which
**nothing currently calls**. The in-app Discord connect flow in world settings goes through
`CreateWebhookSubscriptionStep` directly, authenticated as a world admin, so the user-facing
integration does not depend on this. Unset means those two routes answer 503; set it only when you
want scripted subscription management. It is a machine-to-machine credential — generate it
(`openssl rand -base64 32`), do not choose it. Rotating is safe: nothing persists it, and existing
subscriptions carry their own separate per-delivery signing secret.

⁶ Set as a Fly secret and live in production. When absent, the Discord settings section renders a
"not configured" state rather than failing.

## Production database URLs: pooler vs direct

Three sites name the production database, and **two of them are deliberately different**:

| Site | Endpoint | Mode | Used by |
| --- | --- | --- | --- |
| `webapp/fly.toml` | `ep-withered-truth-a2d65fv7-pooler` | pooled (PgBouncer) | the web app |
| GitHub `production` var `DB_URL` | `ep-withered-truth-a2d65fv7` | direct | Flyway migrations, and the deployed `ingest-scheduler` machine |
| `webapp/scripts/ingest-machine.sh` | *derived from `fly.toml`* | pooled | the manual `--once` smoke test |

Same Neon compute (`br-sweet-base-a241rhcz`, the `master` branch), different connection modes.
Direct for migrations is correct — DDL and session-scoped advisory locks do not want a transaction
pooler in between.

`ingest-machine.sh` used to hardcode a third endpoint, `ep-icy-wood-a2vogqwy-pooler`, which no
longer exists; MCO-334 replaced that with a read of `fly.toml`, so the repo now spells the host
once. Note the remaining asymmetry: `--once` goes through the pooler while the *deployed* scheduler
goes direct, so the smoke test is not a byte-for-byte rehearsal of the nightly run. That gap is
where MCO-348 (session advisory locks through PgBouncer) lives — resolve it there, not here.

## Adding a variable

1. Add the field to `Config` and read it in `readConfig()` (`ConfigLoader.kt`) — nowhere else calls
   `System.getenv`.
2. Mirror the field onto `AppConfig` and its `apply()`.
3. Add a row above.
4. Declare it wherever it needs to exist: `local.env.example`, `test.env`, `fly.toml`,
   `dev.fly.toml`, `prod.yml`, or as a Fly/GitHub secret.
5. Cover the new required/optional combinations in `ConfigLoaderTest`.
